# Git Workflow

## Main Branch

`main` should always contain reviewed and stable project work.

## Feature Branches

Each development task should use its own feature branch.

Examples:

```text
feature/watsonx-storage
feature/orchestrate-setup
feature/assignment-logic
feature/frontend
feature/backend