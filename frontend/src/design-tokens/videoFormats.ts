/**
 * Sprint 2 baseline video formats (UR-VU-05): MP4, MOV, WEBM and AVI. Shared
 * by the drop zone's file-picker filter and the Upload page's format check,
 * so the two can never disagree.
 */
export const ACCEPTED_VIDEO_EXTENSIONS: readonly string[] = [".mp4", ".mov", ".webm", ".avi"];
